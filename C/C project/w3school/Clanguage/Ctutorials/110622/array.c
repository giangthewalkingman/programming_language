#include <stdio.h>
int main(){
    int myNumber[] = { 45, 34, 28, 47};
    printf("%d",myNumber[0]);
    return 0;
}
//myNumber[0] is the first element, then myNumber[1] is the second element in array, index number: nhu la so thu tu cua so do trong tap hop ay :v
